import React from 'react';
import Navigation from './Navigation';

const styles = {
  fontFamily: "sans-serif",
  textAlign: "center"
};

const App = () => (
  <div style={styles}>
    <Navigation />
  </div>
);

export default App;
